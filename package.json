{
  "name": "testpuzzletech",
  "version": "1.0.0",
  "description": "Puzzle Tech Test for Fullstack Developer",
  "main": "index.js",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "start": "concurrently \"cd client && npx vite\" \"cd server && tsc && node dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
